<li class="<%=  self_or_other(chat_message) %>">
  <div class="avatar">
    <img src="http://placehold.it/50x50" />
  </div>
  <div class="chatboxmessagecontent">
    <p><%= chat_message.body %></p>
    <time datetime="<%= chat_message.created_at %>" title="<%= chat_message.created_at.strftime("%d %b  %Y at %I:%M%p") %>">
      <%= message_interlocutor(chat_message).name %> • <%= chat_message.created_at.strftime("%H:%M %p") %>
    </time>
  </div>
</li>